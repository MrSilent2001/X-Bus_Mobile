import {ScrollView, Text} from "react-native";

const LiveLocation = () => {
    return(
        <ScrollView>
            <Text>Live Location</Text>
        </ScrollView>
    );
}

export default LiveLocation;