import {ScrollView, Text} from "react-native";

const OnlinePayment = () => {
    return(
        <ScrollView>
            <Text>Online Payments</Text>
        </ScrollView>
    );
}

export default OnlinePayment;